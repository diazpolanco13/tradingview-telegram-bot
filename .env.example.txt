# ============================================
# TRADINGVIEW MICROSERVICIO V2
# Configuración de entorno
# ============================================

# ----- PUERTO DEL SERVIDOR -----
PORT=3000

# ----- TELEGRAM (Opcional - Solo para webhook V1 legacy) -----
TELEGRAM_BOT_TOKEN=your_telegram_bot_token_here
TELEGRAM_CHAT_ID=your_telegram_chat_id_here

# ----- SUPABASE (REQUERIDO) -----
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your_anon_key_here
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key_here

# ----- REDIS (REQUERIDO para BullMQ) -----
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
# Para Railway/producción con URL completa:
# REDIS_URL=redis://default:password@host:port

# ----- ENCRIPTACIÓN (REQUERIDO) -----
# Genera una key de 32 bytes (64 caracteres hex):
# node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
ENCRYPTION_KEY=your_64_character_hex_encryption_key_change_in_production

# ----- SCREENSHOT SETTINGS -----
SCREENSHOT_TIMEOUT=30000
CHART_LOAD_WAIT=10000
PUPPETEER_EXECUTABLE_PATH=/usr/bin/chromium-browser

# ----- ADMIN TOKEN (OPCIONAL) -----
ADMIN_TOKEN=your_admin_token_here

# ----- ENVIRONMENT -----
NODE_ENV=development
